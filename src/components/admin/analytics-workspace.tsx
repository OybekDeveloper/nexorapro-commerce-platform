import { ArrowUpRight, BarChart3, Download, MousePointerClick, ShoppingCart, Users } from "lucide-react";

import { CategoryChart, RevenueChart } from "@/components/admin/sales-chart";
import { products } from "@/lib/mock-data";

const formatMoney = (value: number) => Math.round(value).toString().replace(/\B(?=(\d{3})+(?!\d))/g, " ");

export function AnalyticsWorkspace() {
  const topProducts = [...products].sort((a, b) => b.sales - a.sales).slice(0, 5);
  const maxSales = Math.max(...topProducts.map((product) => product.sales));
  const funnel = [{ label: "Storefront tashriflari", value: 18420, percent: 100 }, { label: "Mahsulot ko‘rish", value: 11980, percent: 65 }, { label: "Savatga qo‘shish", value: 3684, percent: 20 }, { label: "Checkout", value: 1842, percent: 10 }, { label: "Xarid", value: 1284, percent: 7 }];
  return <div className="space-y-5"><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[["Konversiya", "6.97%", "+1.2%", MousePointerClick], ["Yangi mijozlar", "842", "+14.8%", Users], ["Savat tark etilishi", "30.3%", "-4.1%", ShoppingCart], ["Gross margin", "18.6%", "+2.3%", BarChart3]].map(([label, value, trend, Icon]) => { const Component = Icon as typeof BarChart3; return <article key={String(label)} className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-start justify-between"><span className="inline-flex size-10 items-center justify-center rounded-xl bg-brand/10 text-brand"><Component className="size-[18px]" /></span><span className="inline-flex items-center gap-1 rounded-full bg-brand/10 px-2 py-1 text-xs font-semibold text-brand"><ArrowUpRight className="size-3" />{String(trend)}</span></div><p className="mt-5 text-sm text-muted-foreground">{String(label)}</p><p className="mt-1 text-2xl font-semibold">{String(value)}</p></article>; })}</section><section className="grid gap-5 xl:grid-cols-[minmax(0,1.55fr)_minmax(320px,.75fr)]"><article className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><div className="mb-5 flex items-start justify-between"><div><h2 className="font-semibold">Tushum va buyurtmalar</h2><p className="mt-1 text-sm text-muted-foreground">Oxirgi 30 kun, mln so‘m</p></div><button type="button" className="inline-flex h-9 cursor-pointer items-center gap-2 rounded-xl border border-border px-3 text-xs font-semibold hover:bg-muted"><Download className="size-4" />CSV</button></div><RevenueChart /></article><article className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><h2 className="font-semibold">Kategoriya ulushi</h2><p className="mt-1 text-sm text-muted-foreground">Sotuv qiymati bo‘yicha</p><CategoryChart /><div className="mt-2 grid grid-cols-2 gap-2 text-xs"><div className="rounded-xl bg-muted p-3"><p className="text-muted-foreground">Eng katta ulush</p><p className="mt-1 font-semibold">Smartfon · 48%</p></div><div className="rounded-xl bg-muted p-3"><p className="text-muted-foreground">Eng tez o‘sish</p><p className="mt-1 font-semibold text-brand">Audio · +22%</p></div></div></article></section><section className="grid gap-5 xl:grid-cols-[minmax(0,1.1fr)_minmax(0,1fr)]"><article className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Top mahsulotlar</h2><p className="mt-1 text-sm text-muted-foreground">Sotilgan dona va tushum</p></div><span className="rounded-full bg-brand/10 px-2.5 py-1 text-xs font-semibold text-brand">30 kun</span></div><div className="mt-5 space-y-4">{topProducts.map((product, index) => <div key={product.id}><div className="flex items-center justify-between gap-3 text-sm"><div className="min-w-0"><p className="truncate font-semibold">{index + 1}. {product.name}</p><p className="mt-1 text-xs text-muted-foreground">{product.sales} dona · {formatMoney(product.price * product.sales)} so‘m</p></div><span className="shrink-0 text-xs font-semibold text-brand">{Math.round(((product.price - product.costPrice) / product.price) * 100)}% marja</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-brand" style={{ width: `${Math.round((product.sales / maxSales) * 100)}%` }} /></div></div>)}</div></article><article className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><h2 className="font-semibold">Conversion funnel</h2><p className="mt-1 text-sm text-muted-foreground">Storefront’dan xaridgacha</p><div className="mt-5 space-y-3">{funnel.map((step, index) => <div key={step.label} className="rounded-xl bg-muted/60 p-3"><div className="flex items-center justify-between gap-3 text-sm"><span className="font-medium">{index + 1}. {step.label}</span><span className="font-semibold">{formatMoney(step.value)}</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-background"><div className="h-full rounded-full bg-brand transition-[width]" style={{ width: `${step.percent}%` }} /></div><p className="mt-1.5 text-right text-[10px] font-semibold text-muted-foreground">{step.percent}%</p></div>)}</div></article></section></div>;
}
